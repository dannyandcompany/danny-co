// Allow side-effect imports of global stylesheets, e.g. `import './globals.css'`.
// Next.js normally provides this via next-env.d.ts, but declaring it explicitly
// makes the editor's TS server resolve it regardless of build state.
declare module '*.css';
